- array
  create a number array and then use 'map' to double each value and store it in a new array
  learn about:
    map
    forEach
    filter
    find

- ternary operators
  how they shortcut if/else
  example:
    {isLoading ? <Loading/> : <App/>}

- logical operators
  AND(&&)
  OR(||)

- increment operators
  ++ adds one
  -- subtracts one
  += subtracts value of the right and assigns result
  -= adds value of the right and assigns result

- Object.keys and Object.values